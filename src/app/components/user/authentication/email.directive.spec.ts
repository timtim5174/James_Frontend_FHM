import { EmailDirective } from './email.directive';

xdescribe('EmailDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailDirective();
    expect(directive).toBeTruthy();
  });
});
