import { EqualPasswordsDirective } from './equal-passwords.directive';

describe('EqualPasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualPasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
